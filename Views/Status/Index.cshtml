@model IEnumerable<Task_Managemenet.Models.Status>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StatusType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StatusType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StatusID })
        </td>
    </tr>
}

</table>
